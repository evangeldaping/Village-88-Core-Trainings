(function($) {
  var touch = (function() {
    try {
      return !!(('ontouchstart' in window) || 
          window.DocumentTouch && document instanceof DocumentTouch); // firefox for Android;
    } catch (e) { 
      return false;
    }
 })();

 if (!touch) $('html').addClass('no-touch');
})(jQuery);