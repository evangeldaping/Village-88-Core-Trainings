function celciusToFahrenheit(cDegrees) {
    var cDegrees;
    var fTempVal = (cDegrees * (9 / 5)) + 32;
    console.log(fTempVal);
}

celciusToFahrenheit(0);
celciusToFahrenheit(10);
celciusToFahrenheit(100);